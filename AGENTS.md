# 项目协作约定

## 文档审查

- 以 `.claude/skills/doc-review.md` 为准则，先做全量结构扫描，再按风险逐篇审查概念、示例、版本与安全边界。
- 技术事实、库版本、脚手架命令和官方推荐存在时效性时，先核验官方资料；旧方案保留时必须标明适用范围。

## 文档审查补充

- React/Fiber 教学实现需标注与当前 React 的边界；审查父级查找、遍历推进、更新/删除分支，避免死循环或把组件 fiber 当作 DOM 节点，并说明 key、卸载清理等未覆盖能力。
- 协议与标准类内容优先引用现行 RFC / 官方规范，并将历史 RFC、实验协议和支持有限的机制标为背景而非默认方案。
- 协议的传输特性（复用、长连接、常见端口等）不得表述为安全能力或防火墙绕过；必须单独说明加密、认证、授权和输入校验边界。
- 补充知识保持精简，优先说明职责、数据流、边界条件和常见误区，不扩写成独立教程。
- 跨端内容以能力与维护成本为选型维度，要求将平台特性收敛到适配层，不把单一框架表述为普适答案。
- 文档示例应可独立理解：补齐关键导入、导出、生命周期位置和必要的清理逻辑。
- 异步示例需区分结果竞速与底层任务取消，并说明事件循环由宿主调度回调的边界。
- Web Worker 内容需说明计算职责、消息与 DOM 边界、结构化克隆/所有权转移，并在任务结束或组件卸载时终止 Worker。
- JavaScript 语言特性内容需区分语法糖、内存语义与宿主能力；示例同时遵守 API 返回值和同一作用域唯一声明等运行约束。
- TypeScript 文档要区分静态检查与运行时保障；配置按目标运行时和构建链路给出，不提供脱离上下文的通用 `tsconfig`。
- TypeScript 的重载、继承与兼容性描述必须标明编译期与运行期边界，以及 `strict` 配置和成员可见性等适用前提。
- TypeScript 示例中的泛型和 `keyof` 必须形成真实约束；严格空值语义要区分 `undefined`、可选属性与 `void`。
- 手写 Promise 文档需明确教学实现与原生规范的差异：回调使用微任务，完整解析还涉及 thenable、异常与重复调用保护；数组聚合示例覆盖空输入。
- JavaScript 绑定语义需区分声明建立、TDZ 与对象可变性：`let`/`const` 并非“没有提升”，`const` 只固定绑定。
- 性能和运行时文档不得把特定版本的内部阈值或微优化写成普遍规律；以真实负载的性能录制、堆快照和目标环境数据为准。
- 性能指标需区分实验室诊断与真实用户体验：用 LCP、INP、CLS 观察用户体验，用 TBT 等指标定位问题，并按目标设备和核心路径设预算。
- HTTP 缓存内容需区分允许存储、复用前验证、禁止存储与过期响应；按资源是否可安全复用设计 `Cache-Control` 与验证器。
- TLS 内容不得推荐 SSL、TLS 1.0/1.1、弱密钥或仅保护局部接口；生产站点应全站 HTTPS，私钥与口令不得出现在仓库、命令历史或文档示例中。
- 函数式编程内容应区分纯度、副作用、缺失值和模块优化；示例的缓存、闭包递归与异步失败分支必须保持真实语义。
- 小程序示例按 WXML、WXSS、页面 JS 等真实文件边界拆分代码块，分别标注语言，避免为追求连续展示而牺牲语法高亮与可复制性。
- 卸载阶段的上报示例必须标注为尽力而为；关键数据需由服务端确认、重试或持久化机制保障。
- 部署、安全、认证和存储类文章不得推荐 root 运行、无校验跨域、明文敏感凭据或不可回滚的发布操作。
- 数据库示例必须使用占位强凭据、最小业务权限和受限监听范围；不得示范 `root`、`%`、`*.*` 等广泛账户授权。
- 密码保护内容不得将 MD5、SHA-1、固定盐或前端摘要当作方案；应明确服务端专用慢哈希、独立随机盐与重放边界。
- 安全事件处置先隔离和保留证据，再撤销持久化、轮换凭据、确认范围并清理恢复；下载脚本与二进制需验证来源和校验和，禁止 `777` 权限。
- 安全头示例以 CSP、`X-Content-Type-Options`、Referrer-Policy 与经确认的 HSTS 为主，不将已废弃的 `X-XSS-Protection` 作为防护方案。
- 每批发现的通用问题先同步到 `doc-review.md`，再继续处理下一批文章。
- 小程序授权 API 需区分历史接口与当前用户授权流程；敏感身份数据应由服务端换取或校验，不能在客户端保存密钥。
- 浏览器渲染和空闲调度属于宿主实现，不能把具体时序、帧预算或等待毫秒数写成跨版本保证；应以目标设备实测为准。
- 性能指标需标注实验室指标与真实用户指标的用途，避免继续把已退出核心指标的 TTI 当作唯一结论。
- 构建优化文章中的旧插件和旧框架 API 必须标注版本边界，并给出现代链路的替代方向。

## 持续执行

- 对持续性任务，除非已完成、出现无法自行排除的阻塞，或用户明确叫停，否则持续推进。
- 阶段性进度仅通过评论同步，不使用最终回复中断未完成任务。
- 完成一批修改后执行与风险相称的验证；文档至少检查代码块闭合、标题层级和 diff 空白错误。
